apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: app
  name: app
spec:
  ports:
  - nodePort: 30442
    port: 8000
    protocol: TCP
  selector:
    io.kompose.service: app
  type: NodePort
